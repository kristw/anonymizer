{
  "name": "anonymizer",
  "description": "A utility to remove all field names from json",
  "version": "0.1.0",
  "main": "anonymizer.js",
  "devDependencies": {
    "browserify": "^8.1.3",
    "chai": "*",
    "mocha": "*"
  },
  "scripts": {
    "test": "mocha --no-colors --reporter spec"
  },
  "dependencies": {
    "is-object": "^1.0.1",
    "isarray": "0.0.1"
  }
}
